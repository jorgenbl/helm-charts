categories:
- servicenow
questions:
- variable: persistence.enabled
  description: "Enable StorageClass"
  label: Persistence enabled
  type: boolean
  default: false
  group: "Persistence"
  show_subquestion_if: true
  subquestions:
    - variable: persistence.storageClass
      default: ""
      description: "Select StorageClass"
      type: storageclass
      label: StorageClass
    - variable: persistence.size
      default: "5Gi"
      description: "PVC size"
      type: string
      label: PVC size
- variable: checkmkconf.password
  default: "test123"
  required: true
  description: "Check_MK login password"
  type: password
  label: Password
  group: "Check_MK Configuration"
- variable: checkmkconf.automationsecret
  default: "test123"
  required: true
  description: "Check_MK automation secret"
  type: password
  label: Automation Secret
  group: "Check_MK Configuration"
- variable: checkmkconf.hostport
  default: 8080
  required: true
  description: "Port to expose Check_MK"
  type: string
  label: Port
  group: "Check_MK Configuration"
- variable: servicenow.url
  default: "http://{MID_Server_IP}:{MID_Web_Server_Port}/api/mid/em/jsonv2"
  required: true
  description: "Servicenow MID URL"
  type: string
  label: URL
  group: "Servicenow Configuration"
- variable: servicenow.username
  default: ""
  required: true
  description: "Servicenow API username"
  type: string
  label: Username
  group: "Servicenow Configuration"
- variable: servicenow.password
  default: ""
  required: true
  description: "Servicenow API password"
  type: password
  label: Password
  group: "Servicenow Configuration"
- variable: nginx.username
  default: "foo"
  required: true
  description: "Check_MK reverse proxy basic auth username"
  type: string
  label: Username
  group: "Servicenow Configuration"
- variable: "Nginx Configuration"
  default: "bar"
  required: true
  description: "Check_MK reverse proxy basic auth password"
  type: password
  label: Password
  group: "Nginx Configuration"
- variable: image.pullSecret
  default: "maxdigital"
  required: true
  description: "Registry secret to use"
  type: string
  label: Registry
  group: Registry
